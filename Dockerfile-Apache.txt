# syntax=docker/dockerfile:1# escape=\# check=error=true#Serveur WEBFROM php:8.2-apacheRUN echo 'image basée sur le docker php:8.2-apache'LABEL org.opencontainers.image.authors="Lance2785"LABEL version="1.0"LABEL description="Ce docker contient le site www.besafe.co basé sur un repo github"ENV SITE=www.besafe.coRUN echo 'initialisation des variables'WORKDIR /appRUN echo 'initialisation du dossier de travail'#RUN git clone https://github.com/Lance2785/OCS_P4_ApplicationsADD https://github.com/Lance2785/OCS_P4_Applications.git /appRUN echo 'clonage du repo github'#WORKDIR /app/repository#RUN echo 'nouveau dossier de travail'RUN sudo mkdir /var/www/html/${SITE}/RUN sudo cp /app/repository/Site/html/* /var/www/html/${SITE}RUN sudo a2ensite 01-${SITE}RUN sudo systemctl reload apache2